[binaries]
c = wine_exe + [ msvc_sdk / 'VC/Tools/MSVC' / msvc_version / 'bin/Host' + msvc_build_cpu / msvc_host_cpu / 'cl.exe' ]
c_ld = wine_exe + [ msvc_sdk / 'VC/Tools/MSVC' / msvc_version / 'bin/Host' + msvc_build_cpu / msvc_host_cpu / 'link.exe' ]
ar = wine_exe + [ msvc_sdk / 'VC/Tools/MSVC' / msvc_version / 'bin/Host' + msvc_build_cpu / msvc_host_cpu / 'lib.exe' ]
strip = 'true'
