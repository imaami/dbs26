[host_machine]
kernel = 'xnu'
system = 'darwin'
subsystem = 'macos'

[binaries]
c = [ llvm_c + llvm_suffix, '-arch', macos_host_cpu ]
exe_wrapper = [ 'arch', '-' + macos_host_cpu ]

[properties]
lipo = llvm_lipo + llvm_suffix
strip = llvm_strip + llvm_suffix
